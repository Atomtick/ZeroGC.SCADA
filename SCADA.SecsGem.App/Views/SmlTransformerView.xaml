<UserControl
    x:Class="SCADA.SecsGem.App.Views.SmlTransformerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SCADA.SecsGem.App.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <GroupBox Padding="2,5,2,3" Header="Bytes Presentation">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                    <Button
                        Width="100"
                        Margin="5"
                        Padding="0,4,0,4"
                        Content="Copy" />
                    <Button
                        Width="100"
                        Margin="5"
                        Padding="0,4,0,4"
                        Content="Paste" />
                    <Button
                        Width="100"
                        Margin="5"
                        Padding="0,4,0,4"
                        Content="Clear" />
                    <Button
                        Width="100"
                        Margin="5"
                        Padding="0,4,0,4"
                        Content="Format" />
                    <Button
                        Width="100"
                        Margin="5"
                        Padding="0,4,0,4"
                        Content="Split Header" />
                </StackPanel>
                <TextBox Margin="5" d:Text="09 91 93 95 96 93 09 73" />
            </DockPanel>
        </GroupBox>
    </Grid>
</UserControl>
